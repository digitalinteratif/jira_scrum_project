[pytest]
minversion = 6.0
testpaths = tests
python_files = test_*.py
addopts = 
    --strict-markers 
    -q 
    --tb=short 
    --cov=utils 
    --cov=models 
    --cov-report=term-missing
markers =
    slow: marks tests as slow (deselect with -m "not slow" if desired)
--- END FILE ---